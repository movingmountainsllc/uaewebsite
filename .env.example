# Base URL of the WordPress site (no trailing slash)
WP_BASE_URL=https://uae-construction.com

# Optional — only needed for authenticated endpoints (drafts, writes, private data).
# Generate in WP admin: Users -> Profile -> Application Passwords.
WP_USERNAME=
WP_APP_PASSWORD=
